# Production build for the combined Cloudflare Worker: the SPA and the API share
# one origin, so the API base is empty (requests become relative, e.g. /api/...).
# Picked up automatically by `vite build`.
VITE_API_URL=
