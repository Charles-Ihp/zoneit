[build]
buildCommand = "bun install && bun run build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && bun run start"
healthcheckPath = "/api/users/me"
restartPolicyType = "ON_FAILURE"

[[services]]
name = "backend"
